{
  "name": "treatlab",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "build",
    "package-win": "electron-packager . electron-tutorial-app --overwrite --asar=false --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"TreatLab\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-packager": "^11.1.0",
    "gkm": "^0.2.0",
    "jquery": "^3.3.1"
  },
  "build": {
    "appId": "ou.com",
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    }
  },
  "devDependencies": {
    "electron": "^1.8.4",
    "electron-builder": "^20.5.1",
    "electron-rebuild": "^1.7.3"
  }
}
